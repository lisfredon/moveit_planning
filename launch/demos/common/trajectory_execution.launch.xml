<launch>
  <arg name="robot_type" default="panda"/>

  <!-- Define moveit controller manager plugin: fake, simple, or ros_control -->
  <arg name="moveit_controller_manager" default="fake"/>
  <arg name="fake_execution_type" default="interpolate"/>

  <!-- Flag indicating whether MoveIt is allowed to load/unload  or switch controllers -->
  <arg name="moveit_manage_controllers" default="true"/>
  <param name="moveit_manage_controllers" value="$(arg moveit_manage_controllers)"/>

  <!-- Paramètres génériques d'exécution de trajectoire -->
  <param name="trajectory_execution/allowed_execution_duration_scaling" value="1.2"/>
  <param name="trajectory_execution/allowed_goal_duration_margin" value="0.5"/>
  <param name="trajectory_execution/allowed_start_tolerance" value="0.01"/>


  <include file="$(find moveit_planning)/launch/demos/$(arg robot_type)/$(arg moveit_controller_manager)_moveit_controller_manager.launch.xml"
          pass_all_args="true"/>

</launch>